# DB Configuration
export DB_NAME=
export DB_USER=
export DB_PASSWORD=
export DB_HOST=
export DB_PORT=
export DB_SSLMODE=

# used when deploying staging/production db, as a spearate set of user, password and host is need to connect to remote db, do not export
DB_USER_EXTERNAL=
DB_PASSWORD_EXTERNAL=
DB_HOST_EXTERNAL=
NGROK_AUTH_TOKEN=

# ngrok
NGROK_AUTH_TOKEN=

# Application Configuration
export GIN_MODE=
export ENVIRONMENT=

# aliyun oss
export ALIYUN_OSS_ACCESS_KEY_ID=
export ALIYUN_OSS_ACCESS_KEY_SECRET=

# aliyun fc
export ALIYUN_FC_ACCESS_KEY_ID=
export ALIYUN_FC_ACCESS_KEY_SECRET=
